<input
    class="
        <? if (typeof withIcon !== 'undefined' && withIcon === true) { ?>
            pl-10
        <? } ?>
        placeholder:text-neutral-400 border-grey-500 focus:border-indigo-300 focus:ring-4 ring-indigo-200 w-full rounded-xl border border-solid p-4 outline-none placeholder:text-sm placeholder:font-medium
    "
    placeholder="<?= placeholder ?>"
    type="<?= type ?>"

    <? if (typeof id !== 'undefined') { ?>
        id="<?= id ?>"
    <? } ?>
/>
